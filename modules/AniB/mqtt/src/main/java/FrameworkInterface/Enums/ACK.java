package FrameworkInterface.Enums;

public enum ACK {
      OK,
     ERROR,
     WRONG_CLIENT_ID,
     INVALID_CATEGORY,
     INVALID_DATA,
     INVALID_COMMAND,
     CONNECTION_DENIED,
     MD5_MISMATCH,
     PART_MISSING,
     NA
}
