apply plugin: 'com.android.application'

allprojects {
    repositories {
        maven { url "https://jitpack.io" }
    }
}


android {
    compileSdkVersion 28
    defaultConfig {
        applicationId "fm.ani.fmlauncher"
        minSdkVersion 28
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    signingConfigs {
        debug {
            storeFile     file('/home/tej/Documents/fmSpin/Ani/ClientApp/Android_Ani/fmLauncher/certs/android.keystore')
            storePassword 'anipass'
            keyAlias      'platform'
            keyPassword   'anipass'
        }
        release {
            storeFile     file('/home/tej/Documents/fmSpin/Ani/ClientApp/Android_Ani/fmLauncher/certs/android.keystore')
            storePassword 'anipass'
            keyAlias      'platform'
            keyPassword   'anipass'

            v1SigningEnabled true
            v2SigningEnabled true
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
//            minifyEnabled false
//            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation 'com.github.kaknazaveshtakipishi:PermissionEverywhere:master'
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    implementation 'androidx.appcompat:appcompat:1.0.2'
    implementation "androidx.cardview:cardview:1.0.0"
    implementation "androidx.recyclerview:recyclerview:1.2.1"

    implementation 'androidx.constraintlayout:constraintlayout:1.1.3'
    implementation project(path: ':fmlaunchertypes')
    implementation project(path: ':loop')
    testImplementation 'junit:junit:4.12'
    androidTestImplementation 'androidx.test.ext:junit:1.1.0'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.1.1'
    implementation project(path: ':inputInjector')
    implementation project(':fouriermachines.ani.client.motiongraphics')
    implementation project(path: ':fouriermachines.ani.client.scheduler')
    implementation project(':fouriermachines.ani.client.common')
    implementation project(path: ':kinetictypes')
    implementation project(path: ':kineticsclient')
}
